previewsEnabled: true
#previewsExpireAfterDays: 1
services:
- type: web
  name: containerz
  runtime: image
  image:
    url: docker.io/library/nginx
  plan: free
  autoDeploy: true
  buildFilter:
    ignoredPaths:
    - render.yaml
